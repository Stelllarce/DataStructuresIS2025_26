# Hash Tables & Separate Chaining (Хеш таблици и верижно свързване)

## Какво е Separate Chaining?

**Separate Chaining** (Верижно свързване) е един от основните методи за разрешаване на колизии в хеш таблици. За разлика от **Open Addressing**, където всички елементи се съхраняват директно в масива на таблицата, при верижното свързване всеки индекс от масива (наричан често "bucket" или "кофа") сочи към структура от данни, съдържаща всички елементи, които са хеширани до този индекс.

Най-често тази структура е **свързан списък** (Linked List), но може да бъде и динамичен масив (Vector) или дори балансирано дърво.

**Основна идея:**
```cpp
std::vector<std::list<std::pair<Key, Value>>> table;
```

![Separate Chaining](https://www.researchgate.net/publication/284454241/figure/fig2/AS:391461272080393@1470343048820/Hash-table-representation-for-separate-chaining.png)

### Имплементация в libstdc++ и gcc

[📖 Повече за тази имлементация](https://blog.ilvokhin.com/libstdc++-std-unordered-map/)

![libstdc++](https://i.sstatic.net/qfxPD.png)

### Имплементация на Microsoft STL

![MS STL](https://i.sstatic.net/sNl7p.png)

## Предимства на Separate Chaining

1. **Проста имплементация:** Операциите са интуитивни – просто добавяме или махаме от списък.
2. **Load Factor > 1:** Таблицата може да побере повече елементи, отколкото е размерът на масива (веригите просто стават по-дълги). Това я прави по-устойчива на пикове в броя данни.
3. **Липса на "Clustering":** За разлика от Linear Probing, тук няма групиране на елементи в съседни клетки, което да забавя драстично операциите при лоши хеш функции.
4. **Лесно изтриване:** Изтриването е тривиално (премахване на възел от списък), докато при Open Addressing е необходимо използването на маркери "Tombstone".

## Недостатъци

1. **Допълнителна памет:** Всеки възел в свързания списък изисква допълнителна памет за указател (`next`).
2. **Лоша кеш локалност (Cache Misses):** Тъй като възлите на свързания списък са разпръснати в паметта, обхождането им е по-бавно в сравнение с линейното сканиране на масив (както е при Open Addressing).

## Операции

### Вмъкване (Insert)

1. Изчисляваме хеш индекса: `idx = hash(key) % tableSize`.
2. Търсим в списъка на позиция `idx` дали ключът вече съществува.
   - Ако **да**: обновяваме стойността.
   - Ако **не**: добавяме новия елемент в списъка (обикновено в началото или края).

Сложност: **O(1)** средно (ако приемем, че веригата е къса).

### Търсене (Search / Find)

1. Изчисляваме индекса: `idx = hash(key) % tableSize`.
2. Обхождаме списъка на този индекс елемент по елемент.
3. Ако намерим ключа, връщаме стойността. Ако стигнем края – елементът липсва.

Сложност: **O(1 + λ)**, където λ е средната дължина на веригата.

### Изтриване (Delete)

1. Изчисляваме индекса.
2. Намираме елемента в списъка.
3. Премахваме го от списъка.

Сложност: **O(1 + λ)**.

## Load Factor и Rehashing

При Separate Chaining дефиницията за **Load Factor (λ)** е същата:

```
λ = n / m
```

- **n**: брой елементи
- **m**: размер на таблицата (брой кофи)

Тук обаче **λ** показва **средната дължина на веригите**.
- Ако λ = 5, това означава, че средно във всяка кофа има по 5 елемента.
- Separate Chaining работи дори при λ > 1, но производителността пада линейно.
- Обикновено се прави **Rehashing** (преоразмеряване), когато λ надвиши определена стойност (напр. 1.0, 3.0 или повече, в зависимост от имплементацията).

## Сравнение: Separate Chaining vs Open Addressing

| Характеристика | Separate Chaining (Верижно) | Open Addressing (Отворено) |
|---|---|---|
| **Структура** | Масив + Списъци | Само един масив |
| **Колизии** | Елементите се трупат в кофата | Търси се алтернативна клетка |
| **Load Factor** | Може да расте неограничено (>1) | Трябва да е под 1 (обикновено < 0.7) |
| **Кеш памет** | Лоша (Pointer chasing) | Отлична (последователен достъп) |
| **Памет (Overhead)** | Указатели за всеки елемент | Само неизползваните клетки |

## Задачи от LeetCode

- [**70. Climbing Stairs**](https://leetcode.com/problems/climbing-stairs/)
- [**1. Two Sum**](https://leetcode.com/problems/two-sum/)
